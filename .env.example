NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
GROQ_API_KEY=
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
# X OAuth 2.0 client credentials from the X Developer Portal app settings.
TWITTER_CLIENT_ID=
TWITTER_CLIENT_SECRET=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
# Stripe Dashboard -> Product catalog -> open each product -> copy the Price ID.
STRIPE_SOLO_PRICE_ID=
STRIPE_PRO_PRICE_ID=
RESEND_API_KEY=
ENCRYPTION_KEY=
# Used by Vercel Cron Jobs. Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=
# Set to true only when automatic scheduling is actually configured for the deployment.
NEXT_PUBLIC_AUTO_SCHEDULING_ENABLED=false
NEXT_PUBLIC_APP_URL=https://quill-ai.dev
